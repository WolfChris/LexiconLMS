<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - LexiconLMS</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")
    

    <meta name="description" content="The description of my page" />
</head>
<body>
    <div class="navbar navbar-inverse navbar-fixed-top">
        <div class="container">
            <div class="navbar-header">
                <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                @*@if (User.Identity.IsAuthenticated)*@
                @*{*@
                    @if (User.IsInRole("Teacher"))
                    {
                        <a href="@Url.Action("Index", "Courses")" class="navbar-brand">
                            <img src="~/Img/logo-lexicon@2x.gif" title="Lexicon Logo" alt="Logo Gif" class="logo-gif" />
                        </a>
                    }
                    else if(User.IsInRole("Student"))
                    {
                        <a href="@Url.Action("Index", "Student")" class="navbar-brand">
                            <img src="~/Img/logo-lexicon@2x.gif" title="Lexicon Logo" alt="Logo Gif" class="logo-gif" />
                        </a>
                    }
                    else
                    {
                    <a href="@Url.Action("Login", "Account")" class="navbar-brand">
                        <img src="~/Img/logo-lexicon@2x.gif" title="Lexicon Logo" alt="Logo Gif" class="logo-gif" />
                    </a>

                }
                @*}*@
            </div>
            <div class="navbar-collapse collapse">
                @*<ul class="nav navbar-nav">
                    
                    @if (User.Identity.IsAuthenticated)
                    {
                        if (User.IsInRole("Teacher"))
                        {
                            <li> @Html.ActionLink("Add Course/Module", "Index", "Courses")</li>
                            <li> @Html.ActionLink("Users", "Index", "Users", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
                            <li> @Html.ActionLink("Moduls", "Index", "Moduls")</li>
                            <li> @Html.ActionLink("Activities", "Index", "Activities")</li>
                            <li> @Html.ActionLink("ActivityTypes", "Index", "Activitytypes")</li>
                        }
                    }
                

                @if (User.Identity.IsAuthenticated)
                {
                    if (User.IsInRole("Student"))
                    {
                        <li> @Html.ActionLink("Module", "Index", "Student")</li>
                        <li> @Html.ActionLink("Assigments", "Assigments", "Student")</li>

                    }
                }
                </ul>*@
                @Html.Partial("_LoginPartial")
            </div>
        </div>
    </div>
    <div class="container body-content">
        @RenderBody()
        @*<hr />*@
        <footer>
            <p>&copy; @DateTime.Now.Year - Lexicon .NET Team</p>
        </footer>
    </div>

    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/jqueryval")
    @Scripts.Render("~/bundles/bootstrap")


    @RenderSection("scripts", required: false)
</body>
</html>
